<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %> " style="margin-top: -33px; margin-bottom: -5px;">
    <a href="#" data-dismiss="alert" class="close">×</a>
    <ul>
      <li>
        <%= value %>
      </li>
    </ul>
  </div>
<% end %>